<div class="sidebar-links">
  <ul>
    <% if user_signed_in? %>
      <li><%= link_to "Create an Event", new_event_path %></li>
      <li><%= link_to "User Profile", users_show_path(id: current_user.id) %></li>
  </ul>
    <% else %>
      <li>Please sign in to create an event.</li>
    <% end %>
</div>
